# Large directories that slow build context transfer
target/
.git/
devop/

# Sensitive files
.env


# Local configs that shouldn't be in containers
src/configuration_prod.json

# Dockerfile tests
Dockerfile_*